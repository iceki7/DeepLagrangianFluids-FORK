dataset_dir: "/w/cconv-dataset/ours_default_data" # path to the dataset directory with 'train' and 'valid' subdirs.
train_data:
  random_rotation: true # randomly rotate training data about the gravity axis.
